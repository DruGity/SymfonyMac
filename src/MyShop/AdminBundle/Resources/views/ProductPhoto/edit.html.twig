{% extends "@MyShopAdmin/layout.html.twig" %}

{% block body %}
    <h3>Редактирование фото {{ photo.getTitle() }} </h3>
    <form  method="post" action="{{ path("my_shop_admin.product_photo_edit", {id:photo.getId()}) }}">
        {{ form_widget(form) }}
        <input type="submit" value="Редактировать" />
    </form>
{% endblock %}